# Лямбда-исчисление

```haskell

-- Определение выражения в лямбда-исчислении (почти) по Чёрчу
Exp = Var
      -- переменная
    | (λ Var . Exp)
      -- абстракция (создание функции)
    | (Exp Exp)
      -- аппликация (примение функции к аргументу)

```

## Переменные (в Хаскелле)

```haskell

a
alpha
ユニコード

----

-- символьные имена надо писать в скобках,
-- чтобы использовать их самих по себе
(!)
(?)
($)
(.)

```

## Аппликация

```haskell

-- простейший пример аппликации
(f x)

----

-- вложенная аппликация
(f (g x))
-- Лисп?

----

-- внешние скобки не играют роли, можно не писать
f (g x)

----

-- результат первого применения — функция для второго применения
((f x) y) z

----

-- договоримся, что аппликация ассоциативна слева
f x y z

```

## Абстракция

```haskell

-- простейший пример (лямбда-)абстракции
-- (в Хаскелле мы пишем стрелку вместо точки
-- и бэкслэш вместо лямбды,
-- но всё остальное такое же, как у Чёрча)
(\ x -> y)

----

-- вложенная абстракция
(\ x -> (\ y -> z))

----

-- внешние скобки не играют роли, можно не писать
\ x -> (\ y -> z)

----

-- после применения этой функции получится новая функция
\ x -> (\ y -> (\ z -> a))

----

-- договоримся, что абстракция ассоциативна справа
\ x -> \ y -> \ z -> a

----

-- договоримся, что несколько абстракций подряд можно записывать компактно
\ x y z -> a

```

## Вычисление (редукция)

TODO
